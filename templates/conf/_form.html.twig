{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit"
        class="mt-4 bg-indigo-600 hover:bg-indigo-500 text-white font-semibold text-sm px-6 py-2.5 rounded-lg transition-colors duration-200">
        {{ button_label|default('Envoyer') }}
    </button>
{{ form_end(form) }}
